# ========================
# SERVER CONFIGURATION
# ========================
PORT=5000
NODE_ENV=development

# ========================
# DATABASE CONFIGURATION
# ========================
# For local development:
# MONGODB_URI=mongodb://localhost:27017/wasteManagement

# For MongoDB Atlas (PRODUCTION):
# Replace <username>, <password>, and <cluster-url> with your actual credentials
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/wasteManagement?retryWrites=true&w=majority

# ========================
# JWT CONFIGURATION
# ========================
# Generate secure random strings for production!
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# Admin JWT Secret (separate for better security)
ADMIN_JWT_SECRET=your-admin-jwt-secret-change-this-in-production
ADMIN_JWT_EXPIRE=7d

# ========================
# CORS CONFIGURATION
# ========================
# For local development:
# CORS_ORIGIN=http://localhost:5173

# For production (Vercel):
# CORS_ORIGIN=https://your-app.vercel.app
CORS_ORIGIN=http://localhost:5173

# ========================
# EMAIL CONFIGURATION (Optional)
# ========================
# For Gmail (use App Password, not regular password):
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=noreply@wastemanagement.com

